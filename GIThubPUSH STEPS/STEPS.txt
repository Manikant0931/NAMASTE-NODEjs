STEP 0: Prerequisites (Quick Check)

Make sure:

✅ Git is installed
Open terminal / CMD and run:

git --version


✅ You are logged into GitHub in browser

✅ VS Code terminal is open in your project folder

STEP 1: Open Your Project in VS Code

Open VS Code

Click File → Open Folder

Select your NAMASTE-NODEjs project folder

Open Terminal in VS Code
(Ctrl + ~)

STEP 2: Initialize Git (If not already done)

Run this in terminal:

git init


👉 This creates a .git folder (hidden)

Check status:

git status

STEP 3: Add Remote GitHub Repository

Copy-paste exactly this command:

git remote add origin https://github.com/Manikant0931/NAMASTE-NODEjs.git


Verify:

git remote -v


You should see:

origin  https://github.com/Manikant0931/NAMASTE-NODEjs.git (fetch)
origin  https://github.com/Manikant0931/NAMASTE-NODEjs.git (push)

STEP 4: Add Files to Staging Area

Add all files:

git add .


Check:

git status


You should see files in green (staged)

STEP 5: Commit Your Code
git commit -m "Initial commit for NAMASTE Node.js project"


✔️ Now your code is saved locally in Git

STEP 6: Set Branch to main (IMPORTANT)

GitHub uses main by default:

git branch -M main

STEP 7: Push Code to GitHub 🚀
git push -u origin main